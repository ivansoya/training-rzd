Flask==3.0.3
gunicorn==22.0.0
pytest==8.3.4
# Отчёт машиночитаемым файлом: веб-страница показывает разбор по тестам,
# а не простыню вывода.
pytest-json-report==1.5.0
requests==2.32.3
psycopg2-binary==2.9.10
# Тестам нужен свой ролик: генерируем синтетический вместо того, чтобы держать
# бинарник в репозитории.
av==13.1.0
Pillow==11.0.0
# Единицы проверяют исполнитель графа и геометрию масок. Без библиотеки
# главная проверка — «сборка воспроизводима» — молча пропускалась бы, а она
# ловит ровно то, чего иначе никто не заметит.
numpy==1.26.4
albumentations==2.0.8
opencv-python-headless==4.10.0.84
