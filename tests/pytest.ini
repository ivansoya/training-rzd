[pytest]
# Названия тестов по-русски: отчёт читают те же, кто читает интерфейс.
python_files = test_*.py
python_functions = test_*
addopts = -ra --tb=short
# Пути к исходникам бэкенда и к общим модулям тестов.
pythonpath = /work/backend /work/tests
