const Lab=DesignLab,labPage=document.body.dataset.page;
let openStyle=new URLSearchParams(location.search).get('style')||'studio';if(!Lab.styles.some(s=>s.id===openStyle))openStyle='studio';
const sheet=new URLSearchParams(location.search).get('view')==='sheet';if(sheet)document.body.classList.add('sheet');
function labNav(){return `<header class="lab-header"><a href="index.html" class="lab-brand">Дизайн рабочей среды<span>Вторая итерация</span></a><a href="../index.html">Первая итерация ↗</a></header><nav class="lab-nav"><a href="index.html" class="${labPage==='styles'?'on':''}">Стили</a><a href="palettes.html" class="${labPage==='palettes'?'on':''}">Тёмные палитры · 3</a><a href="screens.html" class="${labPage==='screens'?'on':''}">Все экраны · 19</a><a class="last" href="${Lab.url('editor','studio','graphite')}">Редактор A ↗</a></nav>`}
let labContent='';
if(labPage==='styles')labContent=`<div class="intro"><p class="eyebrow">Новая стилистика для всего приложения</p><h1>Четыре характера.<br>Один рабочий процесс.</h1><p>Типографика, навигация, плотность и форма компонентов меняются вместе. Превью показаны в одинаковой палитре, чтобы сравнивать именно стиль. Цвет можно выбрать независимо.</p></div><div class="style-grid">${Lab.styles.map(s=>`<article class="style-card"><a class="style-preview" href="${Lab.url('overview',s.id,'graphite')}"><img src="previews/style-${s.id}.png" alt="Обзор проекта в стиле ${s.name}. Общая графитовая палитра."></a><div class="style-info"><div class="style-title"><h2>${s.name}</h2><small>${s.tag}</small></div><p>${s.text}</p><div class="traits"><span>${s.font}</span><span>${s.geometry}</span><span>${s.density}</span></div><div class="style-links"><a class="button" href="${Lab.url('projects',s.id,s.palette)}">Пройти по экранам →</a><a class="text-link" href="${Lab.url('editor',s.id,s.palette)}">Редактор A</a><a class="text-link" href="screens.html?style=${s.id}">Карта экранов</a></div></div></article>`).join('')}</div><div class="note"><strong>Редактор остаётся в компоновке A.</strong> Во всех четырёх стилях инструменты слева, кадр в центре, инспектор справа, временная шкала под кадром. Оформление канваса не меняет исходное изображение и расчёт координат разметки.</div>`;
if(labPage==='palettes')labContent=`<div class="intro"><p class="eyebrow">Только тёмные темы</p><h1>Мята. Ледяной.<br>И новый красный.</h1><p>Три отобранных сочетания. Основной акцент отвечает за действия и выбор, второй за сравнение серий и вспомогательную информацию. Цвета классов на изображении остаются независимыми.</p></div><div class="palette-toolbar"><label>Открывать в стиле<select id="open-style">${Lab.styles.map(s=>`<option value="${s.id}" ${s.id===openStyle?'selected':''}>${s.name}</option>`).join('')}</select></label><span class="eyebrow">Контраст подписей и кнопок рассчитан для каждой палитры</span></div><div class="palette-grid">${Lab.palettes.map(p=>`<article class="palette-card"><div class="palette-sample" style="--p-bg:${p.bg};--p-panel:${p.panel};--p-text:${p.text};--p-muted:${p.muted};--p-line:${p.line};--p-accent:${p.accent};--p-accent2:${p.accent2};--p-soft:${p.accent}18;--p-on:${p.on}"><div class="sample-top"><b>Рабочая среда</b><small>Сохранено</small></div><div class="sample-body"><div class="sample-box"><span class="sample-caption">Выбранный набор</span><b class="selected">Варан / базовый</b><span class="sample-button">Открыть</span></div><div class="sample-box"><span class="sample-caption">mAP@50</span><b class="sample-stat">0,873</b><span class="sample-caption">Вторая серия</span></div></div></div><div class="palette-info"><p class="group">${p.group}</p><h2>${p.name}</h2><div class="swatches">${[['bg','Фон'],['panel','Панель'],['text','Текст'],['accent','Акцент'],['accent2','Второй']].map(([k,n])=>`<div class="swatch"><i style="background:${p[k]}"></i><b>${p[k]}</b><small>${n}</small></div>`).join('')}</div><div class="ratios">Подписи ${Lab.ratio(p.muted,p.panel).toFixed(1)}:1 / кнопка ${Lab.ratio(p.on,p.accent).toFixed(1)}:1<br>Второй акцент ${Lab.ratio(p.accent2,p.panel).toFixed(1)}:1</div><a class="button palette-open" data-palette="${p.id}" href="${Lab.url('overview',openStyle,p.id)}">Примерить на экранах ↗</a></div></article>`).join('')}</div><div class="note"><strong>Почему такой красный.</strong> В обсуждениях дизайнеров советуют проверять светлоту красного на тёмном фоне и ограничивать площадь акцента. Для нового варианта подобраны нейтральные поверхности и красный #EF6B73; серебристый второй тон поддерживает графики. Оттенки выбраны для этого макета, а не скопированы как готовый рецепт с форума. <a class="text-link" href="https://www.reddit.com/r/UI_Design/comments/1t1yf53/im_redesigning_my_website_and_was_wondering_how/" target="_blank" rel="noopener noreferrer">Обсуждение оттенков ↗</a> · <a class="text-link" href="https://www.reddit.com/r/UI_Design/comments/1c5hldi/" target="_blank" rel="noopener noreferrer">Красные кнопки в тёмной теме ↗</a><br>Цвета классов и исходное изображение остаются независимыми от палитры.</div>`;
if(labPage==='screens')labContent=`<div class="intro"><p class="eyebrow">Макеты на основе текущих маршрутов и компонентов</p><h1>Весь путь от данных<br>до результатов обучения.</h1><p>Каждый из 19 экранов доступен в четырёх стилях и любой тёмной палитре. Переходы внутри макетов сохраняют выбранное оформление. Названия проектов, кадров и классов узнаваемы; остальные данные демонстрационные.</p></div><div class="flow"><a href="${Lab.url('projects',openStyle,'graphite')}">Проекты</a><span>→</span><a href="${Lab.url('import',openStyle,'graphite')}">Импорт</a><span>→</span><a href="${Lab.url('editor',openStyle,'graphite')}">Разметка A</a><span>→</span><a href="${Lab.url('wizard',openStyle,'graphite')}">Сборка набора</a><span>→</span><a href="${Lab.url('run',openStyle,'graphite')}">Обучение</a></div><div class="table-wrap"><table class="screen-table"><thead><tr><th>Экран и функциональность</th>${Lab.styles.map(s=>`<th>${s.name}</th>`).join('')}</tr></thead><tbody>${Lab.screens.map(s=>`<tr><td><strong>${s.name}</strong><small>${s.note}</small><div class="source-map">${s.source}</div></td>${Lab.styles.map(st=>`<td><a class="button" href="${Lab.url(s.id,st.id,st.palette)}">Открыть ↗</a></td>`).join('')}</tr>`).join('')}</tbody></table></div><div class="note"><strong>Это дизайн-макеты, не реализация приложения.</strong> Фильтры, формы, вкладки, состояния, перетаскивание узлов и геометрия редактора демонстрируются локально. Импорт, сообщения, сохранение на сервере, SAM2 и GPU-задачи не запускаются. Сложные алгоритмы редактора и подготовки данных остаются в существующих компонентах при последующем внедрении.</div>`;
document.getElementById('app').innerHTML=labNav()+`<main class="content">${labContent}</main><footer class="lab-footer"><span>Все файлы изолированы в round-2. Исходники приложения не изменены.</span><span>Выбор: стиль + палитра. Компоновка редактора: A.</span></footer>`;
if(document.getElementById('open-style'))document.getElementById('open-style').onchange=e=>{openStyle=e.target.value;document.querySelectorAll('.palette-open').forEach(a=>a.href=Lab.url('overview',openStyle,a.dataset.palette))};
